// Puede votar o no

edadUduario = parseInt(prompt("Ingrese su edad por favor!"));

if (edadUduario >= 18) {
  alert("Sos mayor de edad, podés ir a votar!!");
} else {
  alert("Sos menor de edad, todavía no podés votar!!");
}
